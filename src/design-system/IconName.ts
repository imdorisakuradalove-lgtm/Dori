/**
 * Cross-platform icon name triple for expo-symbols' SymbolView: SF Symbols
 * on iOS, Material Symbols on Android and web.
 */
export type IconName = {
  ios: string;
  android: string;
  web: string;
};
